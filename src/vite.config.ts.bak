import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        build: './build.ts'
      },
      output: {
        dir: '../dist'
      }
    }
  }
})