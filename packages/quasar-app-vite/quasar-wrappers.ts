// @ts-nocheck
const boot = function (callback) {
  return callback
}

const ssrMiddleware = function (callback) {
  return callback
}

const configure = function (callback) {
  return callback
}

const preFetch = function (callback) {
  return callback
}

const route = function (callback) {
  return callback
}

const store = function (callback) {
  return callback
}

export {
  boot,
  ssrMiddleware,
  configure,
  preFetch,
  route,
  store
}