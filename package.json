{
  "name": "quasar-app-vite-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm run build:quasar-app-vite",
    "build:quasar-app-vite": "cd packages/quasar-app-vite && pnpm run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}